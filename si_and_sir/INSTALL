MANUAL DE INSTALLACIÓN DE LOS PROGRAMAS EN PYTHON

Para la ejecución de este programa se usó Python 3.7 con la versión X de Dash

Los modulos necesarios para el desarrollo fueron:
dash
networkx
pandas
plotly

y pueden ser instalados con pip, ejemplo:

$ pip install pandas

Después de tener los módulos instalados, ejecute cualquier aplicación:

Para el modelo SI:

$ python graph.py

Para el modelo SIR:

$ python graph_sir.py

Al ejecutar dash, la aplicación se lanzará por defecto en localhost, en el puerto 8050 como se muestra en la consola de ejecución, para acceder a ella, asegurese de que el firewall no esté bloqueando la conexión.
