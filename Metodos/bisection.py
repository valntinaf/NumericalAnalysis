#Función para polinomios
def poli(x):
      y=pow(x,2)
      return ()
#Programa principal
print "Este programa encuentra una raíz, por el método de bisección"
float(raw_input('Introduce el valor de xi'))
float(raw_input('Introduce el valor de xs'))
error=float((raw_input('Introduce el error')))

while abs( ) > error:
      xa = (xi+xs)/2.0
if poli(xi)*poli(xa) < 0:
      else:
print"La raíz es: ",xa
